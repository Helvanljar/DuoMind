
# --- Day 4 additions ---
from duomind_app.config import RESEARCHER_PROVIDER, EDITOR_PROVIDER
from duomind_app import llm_orchestrator
@app.get("/api/providers")
def providers():
    return {"researcher": RESEARCHER_PROVIDER, "editor": EDITOR_PROVIDER}
# In /api/session/{session_id}/run handler, use:
#   if mode == "dual":
#       notes, report_md = llm_orchestrator.dual_run(s.user_query, context, verified=True)
#   else:
#       notes = llm_orchestrator.researcher_generate_notes(s.user_query, context)
#       report_md = llm_orchestrator.editor_generate_report(s.user_query, notes, verified)
